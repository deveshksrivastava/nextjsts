import React from "react";

export default function AnyComponents() {
  return (
    <div>
      <h2>NOT WORKING</h2>
      Pages in the components can also be clinet or server components, as it
      will be compined into app.
    </div>
  );
}
